import { ElementDragDirective } from './element-drag.directive';

describe('ElementDragDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementDragDirective();
    expect(directive).toBeTruthy();
  });
});
